export enum EntityType {
  GROWER = "grower",
  DRIVER = "driver",
  CONSUMER = "consumer"
}

export enum DocumentStatus {
  PENDING ="pending",
  REVISION = "revision",
  APPROVED = "approved",
  REJECTED = "rejected",
}
